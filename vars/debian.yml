distribution_packages:
  - cgroup-tools
  - ifenslave
  - liblz4-tool
  - vlan
  - parted
  
debian_11_packages:
  - "linux-tools-{{ ansible_kernel }}"

debian_12_packages:
  - linux-perf
  - rsyslog

ubuntu_18_packages:
  - cgroup-bin
  - systemd-container
  - "linux-tools-{{ ansible_kernel }}"

ubuntu_20_packages:
  - "linux-tools-{{ ansible_kernel }}"

ubuntu_22_packages:
  - "linux-tools-{{ ansible_kernel }}"
